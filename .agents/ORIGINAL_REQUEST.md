# Original User Request

## Follow-up — 2026-06-12T14:29:39Z

Модернизация графической обложки SMM-панели (улучшение текста, удаление сетки, круга и вотермарка, сохранение оригинального разрешения новостных фото), оптимизация ИИ-генераторов фонов (удаление Horde, Unsplash, LoremFlickr) и исправление планировщика автопостинга для стабильной работы на Render.com.

Working directory: d:/SMM
Integrity mode: development

## Requirements

### R1. Очистка HUD-графики на обложках
- Полностью удалить сгенерированную сетку координат (coordinate grid lines & labels) и центральный круг прицела (concentric reticle & crosshairs) с обложки в `_draw_tech_graphics()`.
- Оставить только декоративные угловые рамки (L-brackets), параллельные цепи/линии с узлами (circuit paths) и крайние шкалы (border tick scales) для сохранения стильного минималистичного техно-дизайна.

### R2. Удаление сторонних фото-сервисов и AI Horde
- Полностью удалить устаревшие сервисы `LoremFlickr` и `Unsplash` из цепочки поиска фонов. Удалить метод `fetch_background()` и сопутствующий тест `test_loremflickr_search_params()`.
- Полностью удалить интеграцию с `AI Horde` (метод `generate_horde_background()`).
- Обновить fallback-цепочку генераторов: HuggingFace -> Pollinations.ai -> Cloudflare Workers AI. Если ни один генератор не сработал, использовать процедурный градиент.
- Обновить все места вызова (включая видеогенератор `video_generator.py` и публикатор `telegram_pub.py`) для вызова `generate_ai_background()` вместо `fetch_background()`.

### R3. Публикация оригинальных новостных изображений без обрезки
- Если к новости успешно скачивается оригинальное изображение (`image_url` с сайта новости), бот должен публиковать пост с этим изображением в его **исходном разрешении и качестве**, полностью минуя этап наложения текстовой обложки (`create_cover()`). Это предотвратит некорректную обрезку/центрирование важных деталей.
- Если оригинального изображения нет или скачать его не удалось, бот генерирует AI-обложку с текстом, как и раньше.

### R4. Отключение вотермарка
- Полностью удалить или отключить отрисовку брендированного вотермарка `/ игры ⚡ патчи /` в нижнем правом углу обложки в методе `create_cover()`.

### R5. Премиальный дизайн текста и Russo One
- Изменить загрузку шрифтов: заменить Montserrat-Bold на Russo One (`RussoOne-Regular.ttf`) как основной шрифт заголовка для достижения геймерской/технологической эстетики. Сделать автоматическое скачивание Russo One с Google Fonts при первом запуске, если его нет локально.
- Разработать премиальный UI-дизайн текстового блока:
  - Вместо простого текста с тенью отрисовывать полупрозрачную темную подложку (HUD-карточку) с тонкой рамкой в стиле игры Cyberpunk 2077.
  - Добавить вертикальную неоновую полосу-индикатор акцентного цвета слева от текста для придания глубины.

### R6. Избирательное добавление цитат (blockquote)
- Настроить адаптер `adapter.py` так, чтобы он добавлял цитаты `<blockquote expandable>...</blockquote>` избирательно (примерно в 50-70% постов), если статья длинная и содержит интересное высказывание или инсайт.
- Если новость короткая (мало текста), пост должен генерироваться без блока цитирования.

### R7. Исправление планировщика для Render.com
- Внедрить в фоновый планировщик `scheduler_loop()` в `bot/app.py` проверку времени последнего запуска через БД (`last_pipeline_run` в `app_settings`), чтобы предотвратить частые повторные запуски пайплайна при частых перезапусках контейнера Render.com (когда он просыпается от внешних пингов).
- Установить ограничение на запуск не чаще чем раз в `PARSING_INTERVAL_HOURS` (по умолчанию 3 часа).

## Acceptance Criteria

### Графика и Текст
- [ ] На сгенерированной обложке отсутствуют координатная сетка, центральный круг прицела и вотермарк в нижнем правом углу.
- [ ] Текст обложки рендерится шрифтом Russo One и помещен внутрь стильной полупрозрачной карточки с неоновым индикатором слева.
- [ ] В репозитории отсутствуют упоминания Unsplash, LoremFlickr и AI Horde.

### Публикация и Логика
- [ ] Если в новости есть картинка, пост в Telegram отправляется с оригинальным фото без наложения текста.
- [ ] В Telegram-постах периодически (для длинных новостей) появляются цитаты, а для коротких — отсутствуют.
- [ ] Все 51+ тестов проходят без ошибок (`python -m pytest`).

## Follow-up — 2026-06-13T13:55:59Z

Модернизация отбора цитат на основе длины оригинальных новостей (порог 500 символов), внедрение скрипта отладки ИИ-генераторов изображений и обновление примера конфигурации.

Working directory: d:/SMM
Integrity mode: development

## Requirements

### R1. Понижение порога длины для выборочных цитат
- В `smm_engine/content/adapter.py` изменить порог определения длинных новостей с 800 символов до 500 символов.
- Скорректировать тесты в `tests/test_new_features.py` для тестирования порогов 500 символов.

### R2. Скрипт тестирования ИИ-генераторов
- Создать скрипт `scratch/test_ai_generators.py` для независимой проверки работоспособности Hugging Face, Pollinations и Cloudflare Workers AI.
- Скрипт должен выводить статус по каждому генератору (успех/ошибка), время ответа, наличие ключей в `.env` и сохранять сгенерированные файлы в `temp_media/`.

### R3. Обновление .env.example
- Добавить `HUGGINGFACE_API_KEY` в `.env.example` для улучшения документации конфигурации.

## Acceptance Criteria

### Функциональность
- [ ] Цитаты генерируются для постов средней длины (>500 символов) с вероятностью ~60%.
- [ ] Скрипт `scratch/test_ai_generators.py` успешно запускается и выводит подробную информацию о статусе генераторов.
- [ ] Все тесты `pytest` проходят успешно.
